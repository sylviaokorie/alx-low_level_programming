#include "main.h"

/**
 * _abs - show the absolute value of an integer
 * @c: varible of int
 * Return: 0
 *
 */
int _abs(int c)
{
	if (c >= 48 && c == 48)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
