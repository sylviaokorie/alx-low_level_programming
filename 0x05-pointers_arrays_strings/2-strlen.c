#include "main.h"

/**
 * _strlen - to calculate the length of char
 * @s: integer value
 * Return: Always 0
 *
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; c <= s[c]; c++)
	{

	}
	return (c);
}

