#include "main.h"

/**
 * _strlen - to calculate the length of char
 * @s: integer value
 * Return: Always 0
 *
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{

	}
	return (c);
}

